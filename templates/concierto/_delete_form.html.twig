<form method="post" action="{{ path('concierto_delete', {'id': concierto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concierto.id) }}">
    <button class="btn">Delete</button>
</form>
